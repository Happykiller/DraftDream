// src/pages/Home.tsx
import * as React from 'react';
import { Grid } from '@mui/material';

export function Home(): React.JSX.Element {
  return (<Grid container spacing={3} sx={{ mt: 2 }}><Grid>hello</Grid></Grid>);
}
