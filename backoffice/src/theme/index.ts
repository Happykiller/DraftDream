// src/theme/index.ts
// ⚠️ Comment in English: Minimal theme; extend later (palette/typography/components).
import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: { mode: 'light' },
});
